---
session: 299215fa-4d9f-4dab-8c36-cbc5cea3b0d6
captured: 2026-06-07T18:58:23+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-nitra-cursor/299215fa-4d9f-4dab-8c36-cbc5cea3b0d6.jsonl
---

Логічно — але є один наслідок, який треба зважити.

**Якщо `run_NNN.md` = єдиний артефакт:**

```markdown
---
created_at: ...
actor: agent
status: success | failed
---
## Summary          ← вихідні дані (якщо success)
...
## Reasoning        ← як міркував
## Error            ← опис провалу (якщо failed)
```

Плюси: один файл = одна спроба. Повна картина. Менше файлів.

**Один наслідок:** стан вузла вже не можна визначити за _існуванням_ файлу — потрібно читати frontmatter.

Зараз:
```
є fact_NNN.md → resolved   (file-exists check)
немає         → not yet
```
Після:
```
є run_NNN.md з status: success → resolved   (must-read check)
є run_NNN.md з status: failed  → failed
немає                          → not started
```

Для сканера — не проблема (він читає файли і так). Для `pending-audit_NNN.md` — NNN тепер = NNN `run_NNN.md` (замість `fact_NNN.md`), конвенція та сама.

**Втрата:** пара `plan/fact` стає `plan/run`. Менш очевидно що `run_NNN.md` з success — це "факт".

---

Два шляхи:

**A** — приймаємо `run_NNN.md` як єдиний артефакт, прибираємо `fact_NNN.md` повністю

**B** — лишаємо `fact_NNN.md` як окремий файл тільки для _вихідних даних_ (що споживають наступники), а `run_NNN.md` — тільки метадані спроби; `status` у `run_NNN.md` і посилання `ref: fact_NNN.md` якщо success

A або B?

[package]
name = "agent-core"
description = "Місце ACP-клієнта (Agent Client Protocol) — єдиного транспорту AI-викликів; власного agent loop/provider немає"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "agent_core"
